# SMS adapter interface + BulkSMS implementation (spec §13, D-07):
# send_sms(e164, text) -> provider_message_id, feature-flagged by
# settings.sms_enabled. No models of its own. Out of scope for this
# milestone (models only).
