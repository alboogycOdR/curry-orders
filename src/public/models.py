# Customer-facing views live here (spec §6.1: home, menu, dish, cart,
# checkout, order status, lookup, help, policies, contact). No models of
# its own — the domain model is centralised in core/models.py.
